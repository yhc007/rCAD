[project]
name = "rcad-vision"
version = "0.1.0"
description = "OpenCV vision-inspection microservice for the rCAD process twin"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "opencv-python-headless>=4.9",
    "numpy>=1.26",
]
